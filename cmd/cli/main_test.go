package main_test

import (
	"testing"
)

func TestMainFunc(t *testing.T) {

	// cmd := exec.Command("go", "run", "./...")

	// output, err := cmd.CombinedOutput()
	// if err == nil {
	// 	t.Error("Expected error when missing mandatory env vars, but got none")
	// }
	// if !bytes.Contains(output, []byte("Missing API_TOKEN env var")) {
	// 	t.Errorf("Expected error 'Missing API_TOKEN env var', but got '%v'", err)
	// }

}
